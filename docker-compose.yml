services:
  wlaudio:
    build: .
    ports:
      - "4242:4242"
    volumes:
      - "${HOME}/.claude:/root/.claude:ro"
    environment:
      - PORT=4242
      - HOME=/root
      - TERMINAL_ENABLED=0
